# M1 report — Data layer

> Reconstructed from the build record (the file convention began at M5). M1 is
> commit `55f0a73`, with the adversarial-audit fixes in `4fb8b83`.

Scope: PRD §13 M1 — the compose file, `.env.example`, the §8 schema and first
migration, `lib/db`, `lib/auth`, and the full §10 hand-authored seed.

## What shipped

- PostgreSQL via the committed compose file (locally, portable Postgres 17 since
  Docker is unavailable — §15 decision 1). The §8 schema verbatim with migration
  `20260612133100_init`; `lib/db` singleton; `lib/auth` gains the database-backed
  `getSessionUser` beside the persona resolver. `lib/xp`, `lib/money`, and the
  deterministic Shadow as pure, tested domain modules.
- `prisma/seed-data.ts` is the canonical §10 narrative: 24 employees plus
  personas, 9 accounts, 9 deals across every stage, 3 booking cards, 14 jobs
  with consistent money, 6 courses with 21 finished lessons, 21 channels with
  101 messages (including the seed-time deterministic Shadow draft), the forum,
  the vault, portal content, and a 646-row XP ledger from which user figures are
  computed, never asserted. `lib/mock` re-exports this narrative so the M0
  surfaces and the database cannot diverge.

## Adversarial pass (`4fb8b83`)

Findings resolved across seed, UI, and auth: full lesson coverage so every
outline course ships two finished lessons; second activities and the missing
automatic STAGE_CHANGE rows on single-activity deals; channel/thread posts
relocated to derivable members (PRD 7.3); timeline repairs (dunning arithmetic,
review-before-submission ordering, weekday timestamps, converted-job clamping,
claim-opened deal timing, derived createdAt); the portal guide grown to length.
UI: one canonical job-status tone map, one `LinkPill`, the Today view scoped to
the viewer. Auth: `requireRole` now resolves roles from the User table, not the
static persona list. Stack versions pinned exactly.

## Done-when

`migrate dev && db seed` from scratch succeeds twice (idempotent reset); the
seed-integrity tests pass; the persona switcher swaps between the five seeded
personas. 37 tests green after the audit, build and `tsc` clean.
