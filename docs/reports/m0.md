# M0 report — Alignment & shell

> Reconstructed from the build record. The `docs/reports/` file convention
> began at M5; the M0–M4 reports were delivered in-session, and these notes are
> compiled faithfully from their milestone commits. M0 is commit `d636833`.

Scope: PRD §13 M0 — audit V1 against the spec, install the spec and the
allowlist, lay the design system, convert the tabs to routes.

## What shipped

- `PRD.md` (the v3 build spec) and `CLAUDE.md` (§11) placed at the repo root;
  the §9 dependency allowlist installed.
- The two token scopes per §5.2 — Metapod (dark, digital forest) and Butterfree
  (light, tech lavender) — in `app/globals.css`, with the stock Tailwind palette
  wiped so palette classes fail to compile. The three faces (Schibsted Grotesk,
  Spline Sans Mono, Newsreader) via `next/font`. The two signature flourishes
  (canopy grain, wing hairline) and the chrysalis glyph.
- V1's tabbed dashboard converted to the §6 route tree behind `(employee)` and
  `(client)` route groups, with server-resolved identity (the `krysalis_persona`
  cookie) and the five-persona switcher pill — no client-state theme toggle.
- V1's mock data replaced by a hand-authored V2 narrative in `lib/mock`; every
  emoji and banned phrase from V1 purged.
- M1 foundations laid alongside: the §8 schema + first migration,
  `docker-compose.yml`, `.env.example`, `lib/db`, `lib/xp`, `lib/money`, and the
  deterministic Shadow.

## Done-when

Both themes render the restyled shell with mock data still in place; the §5.10
greps return zero; build and `tsc` green.
